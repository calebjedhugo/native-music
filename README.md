# native-music

```
import { NativeInstrument } from 'native-music'

const triangle = new NativeInstrument({type: 'triangle'})

triangle.play({pitch: 'G4', length: 1000, volume: 'mf'})
triangle.play({pitch: 'F#4', length: 500, volume: .5})
triangle.play({pitch: 'G4'})
```
